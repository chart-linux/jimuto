<%= form_for(request_solution) do |f| %>
  <% if request_solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_solution.errors.count, "error") %> prohibited this request_solution from being saved:</h2>

      <ul>
      <% request_solution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
